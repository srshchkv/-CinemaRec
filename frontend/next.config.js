/** @type {import('next').NextConfig} */
const nextConfig = {
  // вставьте содержимое из next.config.ts без import type
  // пример:
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = nextConfig